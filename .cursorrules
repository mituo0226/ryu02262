# プロジェクト制作ルール

## ページ制作の順序（重要）

1. **最初に `public/index.html` で制作**
   - CSSファイル（`public/styles.css`）を使用可能
   - 通常のWebサイトとして制作
   - Cloudflareなどでの公開を想定

2. **その後、`index-no-css.html` でCSSなし版を制作**
   - `public/index.html` の内容をベースにする
   - すべてのスタイルをインラインスタイル（style属性）に変換
   - `<style>`タグや`<link rel="stylesheet">`は使用禁止
   - 外部CSSファイルの参照は禁止
   - マッチングサイト内システム用（CSSが使えない環境）

## ファイル構成

```
public/
├── index.html      # CSSあり版（メイン）
├── styles.css      # スタイルシート
├── photo/          # 画像フォルダ
│   ├── rogo.png
│   ├── kaede.png
│   ├── kaon.png
│   ├── yukino.png
│   └── sora.png
└── noncss/
    └── index.html  # CSSなし版（未使用の場合は空）

index-no-css.html   # CSSなし版（ルート）
```

## 注意事項

- `public/index.html` を先に制作・更新する
- `index-no-css.html` は `public/index.html` の内容を基に作成
- CSSあり版を変更したら、必ずCSSなし版も同期して更新する

## 作業ディレクトリの指定（重要）

**絶対に守る必要があるルール：**

1. **作業ディレクトリは必ず `C:\Users\mituo\Desktop\kaede` を使用すること**
   - このディレクトリ以外でのファイル編集・作成は禁止
   - `.cursor/worktrees/` 配下のディレクトリでの作業は絶対に禁止

2. **ファイルの作成・編集・削除は必ず実際のプロジェクトディレクトリで行うこと**
   - 作業ディレクトリ: `C:\Users\mituo\Desktop\kaede`
   - このディレクトリが唯一の正しい作業場所

3. **Git操作（コミット・プッシュ）は必ず実際のプロジェクトディレクトリから行うこと**
   - ワークツリー（`.cursor/worktrees/`）からのGit操作は禁止
   - すべてのGit操作は `C:\Users\mituo\Desktop\kaede` から実行

4. **ファイルパスの確認**
   - ファイルを編集する前に、必ず現在の作業ディレクトリを確認
   - パスに `.cursor` や `worktrees` が含まれている場合は、実際のプロジェクトディレクトリに移動してから作業

5. **`.cursor` フォルダは無視**
   - `.cursor` フォルダ内のファイルは編集しない
   - `.gitignore` に `.cursor/` が含まれていることを確認（既に追加済み）